# ============================================================
# Customer Churn Prediction using VQC — Requirements
# Python 3.10 recommended (Qiskit compatibility)
#   pip install --upgrade pip
#   pip install -r requirements.txt
# ============================================================

# ── Core Data & Math ──────────────────────────────────────
numpy>=1.23.0,<2.0.0          # Qiskit has issues with numpy>=2.0
pandas>=1.5.0,<2.2.0
scipy>=1.9.0

# ── Machine Learning ──────────────────────────────────────
scikit-learn>=1.3.0,<1.6.0

# ── Visualization ─────────────────────────────────────────
matplotlib>=3.6.0
seaborn>=0.12.0

# ── Quantum Computing (pinned for compatibility) ──────────
qiskit>=1.1.0,<1.3.0
qiskit-aer>=0.14.0,<0.16.0                  # Statevector / QASM simulator
qiskit-machine-learning>=0.7.2,<0.8.0       # VQC, QSVC, FidelityQuantumKernel
qiskit-algorithms>=0.3.0,<0.4.0             # COBYLA, SPSA, optimizers

# ── Jupyter ───────────────────────────────────────────────
jupyter>=1.0.0
notebook>=7.0.0
ipykernel>=6.25.0
ipywidgets>=8.0.0              # Progress bars inside notebooks

# ── Optional but recommended ──────────────────────────────
tqdm>=4.65.0                   # Training progress display